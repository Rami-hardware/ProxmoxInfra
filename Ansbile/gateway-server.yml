- name: Deploy to Gateway Server
  hosts: gateway-server
  become: true

  roles:
    - DNS
    - docker_setup
    - docker_apps
    - nginx_config
    - adguard
    - crowdsec
    - promtail
    - ufw
