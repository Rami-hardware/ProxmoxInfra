---
- name: Configure K3s server and deploy apps
  hosts: git-k3s-server
  become: true
  vars_files:
    - group_vars/vault.yml
  roles:
    # Docker daemon — used by the CI runner for image builds.
    - docker_setup
    - dns
    - k3s_server
    - k3s_apps
