apiVersion: v1
kind: Service
metadata:
  name: vector-tiles
  labels:
    app: vector-tiles
spec:
  type: NodePort
  selector:
    app: vector-tiles
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: 8080
      nodePort: {{ .Values.nodePort }}
