# Scraparr configuration
# Generated by Ansible

{% if (scraparr_osonarr_api_key | default('')) | length > 0 %}
sonarr:
  url: "{{ scraparr_sonarr.url }}"
  api_key: "{{ scraparr_osonarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if (scraparr_obazarr_api_key | default('')) | length > 0 %}
bazarr:
  url: "{{ scraparr_bazarr.url }}"
  api_key: "{{ scraparr_obazarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if (scraparr_oprowlarr_api_key | default('')) | length > 0 %}
prowlarr:
  url: "{{ scraparr_prowlarr.url }}"
  api_key: "{{ scraparr_oprowlarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if (rscraparr_oadarr_api_key | default('')) | length > 0 %}
radarr:
  url: "{{ scraparr_radarr.url }}"
  api_key: "{{ scraparr_oradarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if (scraparr_ooverseerr_api_key | default('')) | length > 0 %}
overseerr:
  url: "{{ scraparr_overseerr.url }}"
  api_key: "{{ scraparr_ooverseerr_api_key }}"
  api_version: "v3"
{% endif %}
