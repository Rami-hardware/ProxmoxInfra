{% if scraparr_api_version  | length > 0 %}
sonarr:
  url: "{{ scraparr_sonarr.url }}"
  api_key: "{{ scraparr_api_version  }}"
  api_version: "{{ scraparr_api_version }}"
{% endif %}

{% if scraparr_bazarr_api_key | length > 0 %}
bazarr:
  url: "{{ scraparr_bazarr.url }}"
  api_key: "{{ scraparr_bazarr_api_key }}"
  api_version: "{{ scraparr_api_version }}"
{% endif %}

{% if scraparr_prowlarr_api_key | length > 0 %}
prowlarr:
  url: "{{ scraparr_prowlarr.url }}"
  api_key: "{{ scraparr_prowlarr_api_key }}"
  api_version: "{{ scraparr_api_version }}"
{% endif %}

{% if scraparr_radarr_api_key | length > 0 %}
radarr:
  url: "{{ scraparr_radarr.url }}"
  api_key: "{{ scraparr_radarr_api_key }}"
  api_version: "{{ scraparr_api_version }}"
{% endif %}

{% if scraparr_overseerr_api_key | length > 0 %}
overseerr:
  url: "{{ scraparr_overseerr.url }}"
  api_key: "{{ scraparr_overseerr_api_key }}"
  api_version: "{{ scraparr_api_version }}"
{% endif %}
