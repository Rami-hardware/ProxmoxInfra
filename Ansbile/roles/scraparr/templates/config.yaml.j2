{% if sonarr_api_key | length > 0 %}
sonarr:
  url: "{{ scraparr_sonarr.url }}"
  api_key: "{{ sonarr_api_key  }}"
  api_version: "v3"
{% endif %}

{% if bazarr_api_key | length > 0 %}
bazarr:
  url: "{{ scraparr_bazarr.url }}"
  api_key: "{{ bazarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if prowlarr_api_key | length > 0 %}
prowlarr:
  url: "{{ scraparr_prowlarr.url }}"
  api_key: "{{ prowlarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if radarr_api_key | length > 0 %}
radarr:
  url: "{{ scraparr_radarr.url }}"
  api_key: "{{ radarr_api_key }}"
  api_version: "v3"
{% endif %}

{% if overseerr_api_key | length > 0 %}
overseerr:
  url: "{{ scraparr_overseerr.url }}"
  api_key: "{{ overseerr_api_key }}"
  api_version: "v3"
{% endif %}
