# AGENTS.md

Guidance for AI coding agents working in this repo.

## Standing Permissions

- **Always commit and push** changes that belong in the repo after completing a task — the owner has granted standing approval ("always push"). Don't ask first; just do it and say what was pushed. Stage only files you touched — the working tree often has unrelated WIP.

## Repo Overview

Homelab infra: Proxmox VMs provisioned with Terraform, configured with Ansible, apps deployed via GitOps (ArgoCD + Image Updater) on K3s. Branches: `main` / `staging` / `development` — CI runs on push to any of them, plus a monthly scheduled run (full redeploy; regenerates certs as a side effect).

## Load-Bearing Quirks — DO NOT "FIX"

- `Ansbile/` is misspelled on purpose — referenced by workflows, ArgoCD Application CRs, and `.ansible-lint`. Never rename.
- `Terraform/terrraform.tf` (3 r's) is the real terraform block file — at repo root AND inside `modules/proxmox_vm/`. Never rename.
- `.argocd-source-*.yaml` files under `Ansbile/argocd-apps/*/` are auto-generated by ArgoCD Image Updater git write-back. Never hand-edit.
- `Ansbile/group_vars/vault.yml` is referenced by `git-k3s-server.yml` (`vars_files:`) but does not exist in the repo (local-only).
- The k3s server VM's Terraform map key is `github`, not `git-k3s-server` — see `terraform.tfvars`.

## Terraform (`Terraform/`)

- Provider `bpg/proxmox` pinned `~> 0.70.1`. No `required_version`. No remote backend — **state (`terraform.tfstate`) is committed to git**.
- VMs defined as a `map(object)` in `terraform.tfvars` (keys: `gateway`, `media`, `monitoring`, `github`). Map keys are state addresses: **renaming a key destroys the VM**.
  - After a key rename, add the old key to the hardcoded stale-state cleanup list in `deploy.yml` (`for stale in ...`), or CI's plan sees destroy + recreate and blocks.
- `lifecycle.ignore_changes` (in `modules/proxmox_vm/main.tf`) covers `disk`, `initialization`, `clone`, `hostpci`, `cpu` and more — don't remove.
- CI deliberately does NOT apply VMs — plan only, with a destroy/replace guard that fails the pipeline. `terraform apply` in `deploy.yml` is commented out on purpose (manual applies only).
- Manual apply requires `parallelism=1` (Proxmox API race conditions) and `-target` per VM.
- Provider values come from `TF_VAR_*` env vars (GitHub secrets) — never hardcode tokens.

### Commands (run from `Terraform/`)

```bash
terraform init -backend=false
terraform fmt -check -recursive
terraform validate
terraform plan -input=false -no-color -detailed-exitcode -out=tfplan
```

## Ansible (`Ansbile/`)

- Inventory: `inventory.ini` (groups = `gateway-vm`, `media-vm`, `monitoring-vm`, `git-k3s-server-vm`, `host-node`). Group names must match `group_vars/<group>.yml` filenames.
- Playbooks: one per host (`gateway-server.yml`, `media-server.yml`, `monitoring-server.yml`, `git-k3s-server.yml`).
- 19 roles under `Ansbile/roles/`. Deploy order in CI: gateway → monitoring → media → git-k3s.
- Secrets: `group_vars/all.yml` is fully vault-encrypted; also `roles/scraparr/vars/vault.yml`. Vault password via `--vault-password-file ~/.vault_pass` (CI writes it from `ANSIBLE_VAULT_PASSWORD` secret). Never commit plaintext secrets — CI's Trivy scan flags them.

### Conventions (enforced — match existing code)

- **FQCN always**: `ansible.builtin.template`, `community.docker.*` — no short module names.
- **Role vars prefixed** `<role_name>_` (e.g. `k3s_server_version`, `grafana_data_dir`).
- **All config rendered from Jinja2 templates** (`*.yml.j2`) — no inline hardcoded content in tasks.
- **IPs referenced via `hostvars[host]['ansible_host']`** from inventory — single source of truth, never hardcode IPs.
- Command/shell tasks need explicit `changed_when` / `failed_when`.
- Alerting: all Prometheus-evaluated rules live in `roles/prometheus/templates/prometheus-rules.yml.j2` (Alertmanager → alert-forwarder → Discord). Grafana's UI keeps only Loki-datasource (LogQL) rules — add metric alerts to the template, not Grafana.

### Commands

```bash
ansible-galaxy collection install -r Ansbile/requirements.yml
ansible-playbook -i Ansbile/inventory.ini Ansbile/gateway-server.yml --syntax-check --vault-password-file ~/.vault_pass
ansible-lint Ansbile/
ansible-playbook -i Ansbile/inventory.ini Ansbile/gateway-server.yml --check --diff --vault-password-file ~/.vault_pass
```

(`--check` dry run only works on the gateway playbook — gateway and media have Docker tasks that break in check mode.)

## ArgoCD Apps (`Ansbile/argocd-apps/`)

App changes here do NOT need CI — ArgoCD polls and syncs automatically (self-heal + prune).

Top-level `media-app.yaml` / `monitoring-app.yaml` / `network-app.yaml` are the Application CRs (Image Updater annotations, `targetRevision: main`). Per-namespace dir (`media/`, `monitoring/`, `network/`):

- `kustomization.yaml` — `resources:` list + `images:` block with `newTag` (the Image Updater write-back target).
- One plain multi-doc YAML per app (Namespace + PV + PVC + Deployment + Service/NodePort pattern — see `jellyfin.yaml`).
- To add an app: create its YAML, list it in that dir's `kustomization.yaml`. Apps needing secrets would use the Ansible `k3s_apps_list` generic template, but that list is currently **empty** (all apps moved to ArgoCD; `adguard-exporter` was removed — stuck `ImagePullBackOff`).

Excluded from ansible-lint via root `.ansible-lint` (`exclude_paths: [Ansbile/argocd-apps/]`).

## CI/CD (`.github/workflows/`)

Self-hosted runner (small disk — has hit ENOSPC before; workflows prune docker/caches — keep those steps). Entry `main.yml` → reusable workflows: `testing.yml`, `deploy.yml`, `build-image.yml`, `security.yml`, `notify.yml`. Composite actions: `setup-ssh-vault`, `setup-terraform-mirror`, `cleanup-secrets`.

- `build-image.yml` builds the `alert-forwarder` image from `Ansbile/apps/alert-forwarder`, Trivy-scans it, pushes to GHCR.
- Runner has no registry access — Terraform providers install via filesystem mirror (`setup-terraform-mirror`), currently seeded for `bpg/proxmox 0.70.1` and `telmate/proxmox 2.9.14`. Bumping a provider requires updating the mirror action too.
- `setup-terraform` uses `terraform_wrapper: false` — required for `-detailed-exitcode` to work.
- `terraform apply` in `deploy.yml` is commented out on purpose (manual applies only).
- `security.yml` runs Trivy fs (misconfig + secret) in a throwaway container; CVE scanning is disabled (`if: false`).
- Discord notifications via webhook secrets.

## Authoritative Docs

`README.md` is the full architecture reference (VM table, service URLs/ports, scrape jobs, app pattern flags). Mostly accurate; when it conflicts with code, trust the code — e.g. its CI diagram omits the gateway/media playbooks `deploy.yml` actually runs.
